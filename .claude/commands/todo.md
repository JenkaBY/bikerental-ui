---
description: Append a grammar-corrected item to docs/todo.md.
argument-hint: "<todo item text>"
---

Correct the grammar of this input and add it as a new item to `docs/todo.md`: `$ARGUMENTS`

Following Markdown format, append the new todo item to the end of the list. Do not insert blank lines
between the added item and the previous last item. If the file does not exist, create it.
