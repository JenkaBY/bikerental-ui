---
description: Delegate to the team-lead agent to decompose a design file into granular implementation tasks for the dev subagent.
argument-hint: "<path to design.md, e.g. requirements/REQ001/FR01/design.md>"
---

Delegate the following task to the `team-lead` subagent to decompose the design into implementation tasks.

Design file: `$ARGUMENTS`

If no file path is provided, stop and ask the user to supply the path to the `design.md` file.
